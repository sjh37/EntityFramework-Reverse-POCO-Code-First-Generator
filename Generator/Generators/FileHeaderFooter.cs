using Efrpg.Templates;
using System.Text;

namespace Efrpg.Generators
{
    public class FileHeaderFooter
    {
        public readonly string Header;
        public readonly string Namespace;
        public readonly string Footer;

        private readonly string _baseNamespaceName;

        public FileHeaderFooter(string subNamespace)
        {
            var header = new StringBuilder(1024);
            header.AppendLine("// <auto-generated>");

            if (Settings.UseResharper)
                header.AppendLine("// ReSharper disable All");

            if (Settings.NeedsNullForgiving())
                header.AppendLine("#nullable enable");

            if (Settings.UsePragma)
                header.AppendLine("#pragma warning disable 1591    //  Ignore \"Missing XML Comment\" warning");

            foreach (var additionalHeader in Settings.AdditionalFileHeaderText)
            {
                header.AppendLine(additionalHeader);
            }

            Header = header.ToString();

            header = new StringBuilder(500);
            if (Settings.UseNamespace)
            {
                _baseNamespaceName = (Settings.Namespace + subNamespace).Trim().Replace(' ', '_');
                header.AppendLine("namespace " + _baseNamespaceName);
                header.Append("{");
            }

            Namespace = header.ToString();

            var footer = new StringBuilder(30);
            if (Settings.UseNamespace)
                footer.AppendLine("}");
            foreach (var additionalHeader in Settings.AdditionalFileFooterText)
            {
                footer.AppendLine(additionalHeader);
            }
            footer.Append("// </auto-generated>");
            Footer = footer.ToString();
        }

        // Returns the fully-qualified namespace name for the given sub-folder. e.g. "Entities" => "MyProject.Entities"
        public string GetNamespaceName(string subFolder)
        {
            var name = _baseNamespaceName ?? string.Empty;
            if (!string.IsNullOrWhiteSpace(subFolder))
            {
                var folderNs = subFolder.Trim('\\', '/').Replace('\\', '.').Replace('/', '.');
                if (!string.IsNullOrWhiteSpace(folderNs))
                    name = name + "." + folderNs;
            }
            return name;
        }

        // Returns a namespace block with the sub-folder appended. e.g. "Entities" => "namespace MyProject.Entities\n{"
        public string GetNamespaceBlock(string subFolder)
        {
            if (!Settings.UseNamespace)
                return string.Empty;

            var sb = new StringBuilder(500);
            sb.AppendLine("namespace " + GetNamespaceName(subFolder));
            sb.Append("{");
            return sb.ToString();
        }
    }
}